<form>
  <div class="form-group">
    <label>Título</label>
    <input type="text" class="form-control" placeholder="<%= @model.title %>" disabled>
  </div>
  <div class="form-group">
    <label>por: </label>
    <input type="text" class="form-control" placeholder="<%= @postman.name %>" disabled>
  </div>
  <div class="form-group">
    <label>Conteúdo</label>
    <%= simple_form_for([:contributor, @model], multpart: true) do |f|%>
      <%= f.input :body, label: false, as: :ckeditor, input_html: { ckeditor: { height: 1000 } }, disabled: true  %>
    <% end %>
  </div>
</form>
